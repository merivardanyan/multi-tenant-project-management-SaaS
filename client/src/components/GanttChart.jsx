// gantt view - decided to skip this for now, too heavy for v1
// if we add it later: react-gantt-task is the least terrible option
export default function GanttChart() {
  return (
    <div className="flex items-center justify-center h-48 border-2 border-dashed border-gray-200 rounded-lg">
      <p className="text-gray-400 text-sm">Gantt view coming later</p>
    </div>
  );
}
